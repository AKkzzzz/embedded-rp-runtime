# 与当前 RP-Hub 的能力差异

审计基线：

- 最新克隆 RP-Hub：`15525c6`；
- 内嵌运行时模板：0.1 Debug；
- 日期：2026-07-23。

## 结论

内嵌运行时不是“更小的 RP-Hub 皮肤”，而是面向单张卡内应用的第二层运行时。它在状态安全、卡内世界书和应用模块化方面更强；RP-Hub 在完整产品能力、模型接入和成熟度方面明显更强。

现阶段不能笼统宣称模板全面超过 RP-Hub。准确说法是：

> RP-Hub 是成熟的通用宿主；内嵌运行时是更严格、更可编程的卡内应用内核。

最新 RP-Hub 会把破限、辅助预设、Style Priority、用户资料、工具协议和 UI Template 上下文合并进系统提示，并在请求前执行 Prompt Regex。模板现在已经把 Style Priority、卡内 Prompt/Output Regex、逐轮 classicMemory 和三类卡内工具协议接入；用户资料、宿主 UI Template 上下文仍按单舞台设计刻意不复制。

因此当前不能宣称“模型看到的消息与 RP-Hub 完全一致”。模板在状态约束、事务写入、显式递归和单舞台生命周期上更强，但生成文风、工具反应与部分上下文仍可能不同。

## 能力矩阵

| 系统 | RP-Hub | 内嵌运行时 | 当前判断 |
|---|---|---|---|
| API 供应商与 Key 管理 | 完整 | 复用同源官方设置，不另存 Key | 设置体验对齐，配置管理仍由宿主负责 |
| 模型列表与切换 | 完整 | 同源读取官方设置并支持 route | 基础路径已对齐 |
| 流式生成 | 已有 | 已实现 SSE、JSON 与强制 SSE 兼容 | 基础路径已对齐 |
| 聊天历史与楼层 | 完整 | 卡内持久历史、停止、继续、重生成、编辑、删除、清空 | 单舞台核心已完成 |
| 预设管理 | 角色、启停、排序、编辑、导入导出 | 官方默认、角色、阶段、启停、排序、编辑、导入导出、来源诊断 | 默认内容对齐，模板额外保留阶段和来源元数据 |
| 世界书扫描 | 最近历史、概率、全局/角色作用域 | 已对齐历史、概率、作用域和诊断 | 核心触发语义对齐 |
| 世界书位置 | 七类位置和指定历史深度 | 七类位置和指定历史深度 | 已对齐 |
| 世界书递归 | 当前生成路径不递归 | 可关闭的显式依赖、有界深度、去重 | 模板提供确定性扩展 |
| 世界书预算 | 不单独裁剪命中条目 | 默认不裁剪，可由应用包启用字符预算 | 默认兼容，复杂卡可收紧 |
| 模型写世界书 | 无卡内事务协议 | proposal、revision、证据、锁定条目 | 模板更强 |
| 状态更新 | 原生变量更新模型，部分 JSON 合并 | schema、只读、枚举、范围、未知字段、事务 | 模板更严格 |
| 向量记忆 | embedding、int8 量化、Top K、主动检索和向量模式历史裁剪 | 卡内 IndexedDB、int8:maxabs:v1、Top K、关键词加权、历史裁剪、后台巡检、指数退避重试和 `<tool_memory:…>` 主动检索 | 量化与基础巡检语义已对齐；RP-Hub 的批量 UI 和并发调度仍更成熟 |
| 总结记忆 | `classicModel` 逐轮总结，旧 AI 消息由摘要替换 | 默认关闭；开启后按逐轮目标总结，保留 user、以 classicMemory 替换旧 assistant，再注入最近楼层 | 语义已基本对齐，批量巡检/并发仍未复制 |
| Prompt Regex | 请求前/显示后按脚本顺序处理 | 卡内 Regex Registry，区分 prompt/output、频道和深度，最终正文经过 output 阶段 | 卡内脚本可控；尚未复制全部宿主编辑器 |
| 主动工具协议 | 向量、关键词、Tavily、结果回填和多轮续写 | 新增“工具”页；向量、关键词、联网三项可独立开关，最多自动续写4轮，结果用 `active_tool_results` 回填 | 协议形状对齐；联网需要宿主受控搜索桥 |
| 插件生态 | 宿主级功能较多，但非卡内 SDK | 本地插件清单、权限、依赖、顺序、清理；递归与模型补丁守卫已有真实实现 | 模板更适合卡内模块，但插件数量仍少 |
| 命令系统 | `triggerSlash` 和宿主命令 | 仅桥接，注册系统待完成 | RP-Hub 更强 |
| Renderer | 聊天和可执行 HTML | Galgame、电子书、战棋可插拔接口 | 模板方向更适合复杂卡 |
| Prompt 诊断 | 控制台日志与部分面板 | 世界书、预设、状态、记忆、插件统一快照 | 模板更集中 |
| 移动端 | 完整宿主界面 | Debug 和应用容器独立响应式 | 目标不同 |
| 安全边界 | 可执行卡 iframe 可读取同源设置 | 只在请求闭包使用 Key，诊断与存档脱敏 | 模板增加产物审计 |

## RP-Hub 预设的实际注入逻辑

当前 RP-Hub 支持：

- `system`、`user`、`assistant` 三种预设角色；
- `破限`作为最前系统提示；
- 其他 system 预设合并为辅助约束；
- user/assistant 预设作为历史前的预注入消息；
- 启停、拖动排序、编辑、导入和导出。

模板对应为：

```text
system-root
  → 破限

worldbook
  → system-top / recursive dependencies

system-support
  → 防抢话
  → 防神化
  → 人格内核
  → 文风
  → 视角等

prelude
  → User 1
  → Assistant 1
  → User 2
  → Assistant 2
```

相比 RP-Hub 依赖预设名称判断“破限”，模板使用显式 `phase`，应用包可以增加自己的阶段而不修改 Prompt Compiler。

## 默认预设对齐

模板不再维护筛选或改写版本。全部官方预设按官方顺序、角色和启停状态内置；卡内运行时只负责把“破限”映射到 `system-root`，把其他 system 项映射到 `system-support`，把 user/assistant 项映射到 `prelude`。

## 下一阶段优先级

1. 宿主受控的联网搜索桥；
2. Command Registry；
3. 首个 Galgame 或战棋应用包接入。
